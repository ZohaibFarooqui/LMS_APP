import 'dart:async';

class BiometricService {
  Future<bool> isBiometricAvailable() async {
    // Placeholder for actual biometric availability check.
    return true;
  }

  Future<bool> authenticate() async {
    // Integrate with local_auth in future iteration.
    return true;
  }
}

